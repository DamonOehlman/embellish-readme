#!/usr/bin/env node
const path = require('path');
const fs = require('fs');
const { embelish } = require('..');

embelish({ filename: path.resolve(process.argv[2]) });

// const parsed = nopt(knownOpts, shorthands, process.argv, 2);
// const targetFile = path.resolve(parsed.argv.remain[0] || '');

// function generate(input) {
//   // include the input into the parsed options
//   parsed.input = input;

//   // run the gendocs processor
//   require('../')(parsed, function(err, output) {
//     if (err) {
//       return process.exit(1);
//     }

//     console.log(output);
//   });
// }

// fs.stat(targetFile, function(err, stats) {
//   if (err || (! stats.isFile())) {
//     return generate();
//   }

//   fs.readFile(targetFile, { encoding: 'utf8' }, function(err, content) {
//     if (err) {
//       return console.error('Could not read file: ' + targetFile);
//     }

//     generate(content);
//   });
// });
