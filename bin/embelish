#!/usr/bin/env node
const path = require('path');
const fs = require('fs');
const { embelish } = require('..');

embelish({ filename: path.resolve(process.argv[2]) }).then(content => console.log(content));
